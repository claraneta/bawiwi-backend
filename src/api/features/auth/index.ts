import { Router } from 'express';

const router = Router();

// Auth feature routes will be added here
// e.g. router.post('/login', loginController);
// e.g. router.post('/register', registerController);

export default router;
